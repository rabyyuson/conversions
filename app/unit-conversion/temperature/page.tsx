import Unit from '@/app/unit-conversion/unit';

/**
 * Renders the Unit component for temperature conversion.
 * 
 * @returns The Unit component specifically for temperature conversion
 */
export default function Page() {
    return (
        <Unit type='temperature' />
    );
}
