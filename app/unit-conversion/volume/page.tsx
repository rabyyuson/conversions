import Unit from '@/app/unit-conversion/unit';

/**
 * Renders the Unit component for volume conversion.
 * 
 * @returns The Unit component specifically for volume conversion
 */
export default function Page() {
    return (
        <Unit type='volume' />
    );
}
